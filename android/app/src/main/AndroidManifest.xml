<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Package/namespace now lives in android/app/build.gradle's
         `namespace` field (AGP 8 convention) — do not add it back here. -->
    <!-- Camera for receipt capture -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <!-- Reading picked images from gallery (scoped storage on Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>

    <!-- Only needed if the user targets a legacy (<API 29) write path for Excel export -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>

    <!-- Internet access for the Google AI Studio (Gemini) API and
         Microsoft Graph (OneDrive/SharePoint Excel) — HTTPS only. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- No custom `android:name` here: this project uses Flutter's v2
         Android embedding (see flutterEmbedding meta-data below), whose
         FlutterActivity initializes itself without a custom Application
         subclass. The old `io.flutter.app.FlutterApplication` class this
         previously pointed to belongs to the v1 embedding and no longer
         exists in current Flutter engines — leaving it in would crash
         the app on launch. -->
    <application
        android:label="receiptscanner"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Displays an Android View that continues showing the launch screen
                 Drawable until Flutter paints its first frame, then this splash
                 screen fades out. A splash screen is useful to avoid any visual
                 gap between the end of Android's launch screen and the painting of
                 Flutter's first frame. -->
            <meta-data
              android:name="io.flutter.embedding.android.SplashScreenDrawable"
              android:resource="@drawable/launch_background"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Required by msal_auth (Microsoft sign-in for OneDrive/
             SharePoint Excel access) to capture the OAuth redirect.
             Replace YOUR_BASE64_SIGNATURE_HASH below with the SAME value
             you use for _redirectUriAndroid in onedrive_auth_service.dart
             (URL-encoded base64 SHA1 of your signing certificate) — see
             that file's doc comment for the exact keytool/openssl command. -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="msauth"
                    android:host="${applicationId}"
                    android:path="/AIqW%2Fh3D%2Fyo%2BBStSPqBZvoQK7Xc%3D"/>
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
